# Тестирование Django-проектов YaNote и YaNews

Проект посвящён автоматизации тестирования и рефакторингу тестовой базы для двух Django-приложений: **YaNote** (заметки) и **YaNews** (новости). Основные задачи: обеспечение качества, повышение надёжности кода и модернизация legacy-тестов с `unittest` на современный стек `pytest`.

## 📌 Содержание
1. [Описание проектов](#описание-проектов)
2. [Цели тестирования](#цели-тестирования)
3. [Структура тестов](#структура-тестов)
    - [YaNote (unittest)](#yanote-unittest)
    - [YaNews (pytest)](#yanews-pytest)
4. [Рефакторинг тестовой базы](#рефакторинг-тестовой-базы)
5. [Запуск тестов](#запуск-тестов)
6. [Технологический стек](#технологический-стек)

---

## Описание проектов

- **YaNote** — сервис для создания и управления персональными заметками. Включает функции создания, редактирования, удаления и маркировки заметок как выполненных.
- **YaNews** — новостной агрегатор с возможностью комментирования. Пользователи могут читать новости, добавлять комментарии, а авторы — управлять своими комментариями.

## Цели тестирования

1. **Проверка доступности страниц** для анонимных и аутентифицированных пользователей.
2. **Валидация бизнес-логики**: права доступа, уникальность данных, автоматическая генерация полей.
3. **Проверка корректности отображения контента**: сортировка, фильтрация, передача данных в шаблоны.
4. **Тестирование форм**: создание, редактирование, валидация (включая кастомную, например, фильтрацию запрещённых слов).
5. **Модернизация тестовой базы**: переход с `unittest` на `pytest` для улучшения читаемости, поддержки и скорости написания тестов.

## Структура тестов

### YaNote (unittest)

Тесты организованы по трём основным файлам, каждый из которых отвечает за отдельный аспект тестирования.

#### `test_routes.py`
- Доступность главной страницы для всех пользователей.
- Доступность страниц заметок (`примечания/`, `выполнено/`, `add/`) только для аутентифицированных пользователей.
- Страницы отдельных заметок, редактирования и удаления доступны **только автору**. Для других пользователей возвращается ошибка 404.
- Анонимные пользователи перенаправляются на страницу входа при попытке доступа к защищённым страницам.
- Страницы регистрации, входа и выхода доступны всем.

#### `test_content.py`
- Проверка передачи отдельной заметки в контексте шаблона со списком заметок (`object_list`).
- В списке заметок пользователя отсутствуют заметки других пользователей.
- На страницы создания и редактирования заметки передаются соответствующие формы.

#### `test_logic.py`
- Аутентифицированный пользователь может создавать заметки, анонимный — нет.
- Невозможно создать две заметки с одинаковым `slug`.
- Автоматическая генерация `slug` из заголовка с помощью `pytils.translit.slugify`, если поле не заполнено.
- Пользователь может редактировать и удалять **только свои** заметки.

### YaNews (pytest)

Тесты написаны с использованием `pytest` и используют фикстуры для создания тестовых данных и клиентов.

#### `test_routes.py`
- Главная страница и страница отдельной новости доступны анонимному пользователю.
- Страницы редактирования и удаления комментариев доступны **только автору** комментария.
- Анонимный пользователь перенаправляется на страницу авторизации при попытке доступа к защищённым страницам.
- Авторизованный пользователь не может редактировать или удалять чужие комментарии (ошибка 404).
- Страницы регистрации, входа и выхода доступны анонимным пользователям.

#### `test_content.py`
- На главной странице отображается не более 10 новостей.
- Новости отсортированы от самой свежей к самой старой (свежие в начале списка).
- Комментарии на странице новости отсортированы в хронологическом порядке: старые в начале, новые в конце.
- Форма для отправки комментариев на странице новости доступна только авторизованным пользователям.

#### `test_logic.py`
- Анонимный пользователь **не может** отправить комментарий.
- Авторизованный пользователь **может** отправить комментарий.
- Комментарии, содержащие запрещённые слова, не публикуются, а форма возвращает ошибку.
- Авторизованный пользователь может редактировать и удалять **только свои** комментарии.

## Рефакторинг тестовой базы

### Задача
Модернизация legacy-тестовой базы на `unittest`, которая была сложна в поддержке и расширении, с переходом на `pytest` и увеличением покрытия критической логики.

### Выполненные работы
- Проведён детальный анализ существующих тестов для обоих проектов.
- **Переписано 14 тестов** с `unittest` на `pytest` с активным использованием **фикстур** и **параметризации**.
- Разработаны тесты для сложной бизнес-логики, включая проверку прав доступа для разных ролей пользователей.
- Созданы тесты для **кастомной валидации** (фильтрация запрещённых слов в комментариях).
- Реализованы **интеграционные тесты** для проверки корректности сортировки новостей и комментариев.
- Разработаны тесты для работы с формами и проверки контекста, передаваемого в шаблоны.

### Результаты
- **Успешный переход на `pytest`**, что значительно упростило чтение, поддержку и расширение тестов.
- **Сокращение времени на написание новых тестов на 40%** благодаря использованию фикстур и параметризации.
- **Существенное снижение риска регрессий** за счёт расширенного покрытия критической бизнес-логики.

## Запуск тестов

### Предварительные требования
- Python 3.8+
- Установленный `pip`

### Установка
1. Клонируйте репозиторий и перейдите в его директорию:
   ```bash
   git clone git@github.com:altdinov/django_testing.git
   cd django_testing
